<?php
echo "Симулятор Cron запущен в фоне. Нажмите Ctrl+C для остановки.\n";

// Бесконечный цикл — точно так же работают настоящие демоны
while (true) {
    // Вызываем ваш оригинальный скрипт task.php
    include 'task.php';
    
    echo "[" . date('H:i:s') . "] Скрипт task.php успешно выполнен.\n";
    
    // Засыпаем ровно на 60 секунд (1 минуту) перед следующим запуском
    sleep(60); 
}
